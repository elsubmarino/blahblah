<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.shoes.mapper.ProductMapper">
	<select id="showMeTheProduct" resultType="com.shoes.domain.ShoesMain"
		parameterType="Integer">
		select * from shoes_main where product_id=#{num}
	</select>
</mapper>